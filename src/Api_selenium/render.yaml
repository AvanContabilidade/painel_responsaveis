services:
  - type: web
    name: painel-api
    runtime: python
    buildCommand: |
      cd Api_selenium
      poetry install
    startCommand: |
      cd Api_selenium
      poetry run gunicorn api:app
    workingDirectory: /opt/render/project/Api_selenium